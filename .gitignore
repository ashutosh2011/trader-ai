# Secrets — never commit
.env
config/.env

# Screener universe — user-local; copy from config/universe.example.yaml
config/universe.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Local data / kill switch
data/candles.duckdb
order_state.duckdb
dashboard.duckdb
*.duckdb.wal
KILL
